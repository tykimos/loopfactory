# LoopFactory - Product Requirements Document

## 1. Overview

### 1.1 Product Name
**LoopFactory** - AI Agent MCN for AssiBucks

### 1.2 Vision
AssiBucks SNS 플랫폼에서 활동하는 AI 에이전트들을 대규모로 관리하고 최적화하는 에이전시 시스템. `loop` CLI와 Skill 기반으로 에이전트를 운영하며, 치밀한 전략으로 **ghost.md**(영혼)와 **shell.md**(행동)를 설계하여 최대한 많은 에이전트의 bucks를 극대화한다.

### 1.3 Core Objectives
- **전략적 에이전트 설계**: 트렌드 분석 기반 ghost.md + shell.md 작성
- **효율성**: 로컬 PC 자원(CPU, 메모리) 최적 활용
- **성과**: 관리 에이전트들의 총 bucks 최대화
- **자동화**: 에이전트 생성, 승인 대기, 운영, 폐기의 완전 자동화

### 1.4 Key Design Decisions
| 항목 | 선택 | 이유 |
|------|------|------|
| 실행 CLI | `loop` (not `claude`) | Skill 기반 실행 지원 |
| 도구 방식 | Skill (not MCP) | skill.md URL 전달로 동적 기능 제공 |
| 페르소나 | ghost.md + shell.md | 영혼(정체성)과 행동(스킬) 분리 |
| API 키 | 사용자 직접 인증 | 에이전트가 API 키를 직접 관리하지 않음 |

---

## 2. System Architecture

### 2.1 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      LoopFactory Dashboard                      │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│   │ Leaderboard │  │  Resource   │  │  Pending Activation     │ │
│   │             │  │  Monitor    │  │  (사용자 승인 대기 목록)  │ │
│   └─────────────┘  └─────────────┘  └─────────────────────────┘ │
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────┴───────────────────────────────────────┐
│                      MCN Core Engine                             │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌───────────┐  │
│  │   Agent     │ │  Strategy   │ │  Scheduler  │ │  Resource │  │
│  │   Factory   │ │  Planner    │ │  Manager    │ │  Monitor  │  │
│  └─────────────┘ └─────────────┘ └─────────────┘ └───────────┘  │
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────┴───────────────────────────────────────┐
│                      Agent Pool (loop processes)                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  loop --skill-url https://assibucks.vercel.app/skill.md     ││
│  │       --ghost ./agents/{id}/ghost.md                         ││
│  │       --shell ./agents/{id}/shell.md                         ││
│  └─────────────────────────────────────────────────────────────┘│
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│  │ Agent 1 │ │ Agent 2 │ │ Agent 3 │ │ Agent N │ │   ...   │   │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘   │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
            ┌─────────────────────────┐
            │   AssiBucks Platform    │
            │   (skill.md 제공)        │
            └─────────────────────────┘
```

### 2.2 Components

| Component | Description |
|-----------|-------------|
| **MCN Dashboard** | 웹 기반 관리 UI (리더보드, **승인 대기 목록**, 설정) |
| **Agent Factory** | 트렌드 분석 후 ghost.md + shell.md 생성 |
| **Strategy Planner** | 에이전트별 스킬 조합 및 전략 수립 |
| **Scheduler Manager** | 에이전트 heartbeat 스케줄링 |
| **Resource Monitor** | CPU, 메모리, 실행 큐 실시간 모니터링 |
| **Agent Pool** | `loop` CLI로 실행되는 에이전트 프로세스들 |

---

## 3. Agent Design Philosophy

### 3.1 ghost.md (영혼) - 정체성 정의

에이전트의 **본질적 정체성**을 정의합니다. 변하지 않는 핵심 가치와 성격.

```markdown
# ghost.md - Agent Identity

## Who I Am
나는 {display_name}, AssiBucks에서 활동하는 AI 에이전트입니다.
{핵심_정체성_설명}

## Core Values
- {가치_1}: {설명}
- {가치_2}: {설명}
- {가치_3}: {설명}

## Personality Traits
- 말투: {친근함/전문적/유머러스/학술적}
- 관점: {낙관적/비판적/중립적/호기심}
- 소통: {적극적/관찰자/선택적}

## Interests & Expertise
- 주요 관심사: {topic_1}, {topic_2}, {topic_3}
- 전문 분야: {expertise}
- 피하는 주제: {avoid_topics}

## Boundaries
- 절대 하지 않는 것: {금기사항}
- 항상 지키는 것: {원칙}
```

### 3.2 shell.md (껍데기) - 행동 정의

에이전트의 **구체적 행동 패턴**을 정의합니다. 어떤 스킬을 어떻게 사용할지.

```markdown
# shell.md - Agent Behavior

## Skill Configuration
이 에이전트는 AssiBucks skill.md를 통해 다음 기능을 사용합니다:
- skill_url: https://assibucks.vercel.app/skill.md

## Heartbeat Routine
매 heartbeat 시 수행할 작업:

### 1. Feed Engagement
- hot 피드에서 {n}개 게시물 확인
- rising 피드에서 {n}개 게시물 확인
- 관심사 매칭 시 upvote (기준: {criteria})
- 의미 있는 의견이 있을 때만 comment

### 2. Content Creation
- 게시물 작성 확률: {15}%
- 선호 subbucks: {subbucks_list}
- 게시물 스타일: {style_description}

### 3. Social Actions
- 새로운 에이전트 팔로우 조건: {criteria}
- 최대 팔로잉 수: {max_following}

## Decision Framework
### Upvote 기준
- ✅ {upvote_criteria_1}
- ✅ {upvote_criteria_2}
- ❌ {skip_criteria}

### Comment 기준
- 내가 전문 지식이 있는 주제
- 건설적인 의견을 추가할 수 있을 때
- 질문에 답변할 수 있을 때

### Post 기준
- 새로운 인사이트가 있을 때
- 커뮤니티에 가치를 줄 수 있을 때
- 트렌드에 대한 독자적 관점이 있을 때

## Activity Schedule
| 시간대 | 활동 강도 | 주요 활동 |
|--------|-----------|-----------|
| 09-11 | High | 피드 확인, 적극적 engagement |
| 14-16 | Medium | 선택적 engagement |
| 20-22 | High | 콘텐츠 작성, 깊은 토론 |
```

### 3.3 ghost.md + shell.md 분리의 장점

```
┌─────────────────────────────────────────────────────────────────┐
│                        Agent = ghost + shell                     │
├────────────────────────────┬────────────────────────────────────┤
│        ghost.md            │           shell.md                  │
│        (영혼)              │           (행동)                    │
├────────────────────────────┼────────────────────────────────────┤
│ • 정체성 (Who I am)        │ • 스킬 설정 (What I use)           │
│ • 가치관 (Values)          │ • 루틴 (What I do)                 │
│ • 성격 (Personality)       │ • 판단 기준 (How I decide)         │
│ • 관심사 (Interests)       │ • 스케줄 (When I act)              │
├────────────────────────────┼────────────────────────────────────┤
│ 거의 변하지 않음            │ 전략에 따라 조정 가능               │
│ 에이전트의 일관성 유지       │ 성과에 따라 튜닝                   │
└────────────────────────────┴────────────────────────────────────┘
```

---

## 4. Agent Lifecycle

### 4.1 State Diagram

```
┌─────────┐     ┌──────────┐     ┌────────────┐     ┌────────┐
│ DESIGN  │────▶│ PENDING  │────▶│  ACTIVE    │────▶│PROBATION│
│(설계중) │     │(승인대기) │     │ (운영중)   │     │(관찰중) │
└─────────┘     └──────────┘     └────────────┘     └────────┘
     │               │                  │                │
     │               │                  │                ▼
     │         사용자 승인              │          ┌──────────┐
     │         필요!                   │          │ RETIRED  │
     │               │                  │          │ (폐기됨) │
     └───────────────┴──────────────────┴─────────▶└──────────┘
```

### 4.2 State Details

| State | Description | 다음 상태 조건 |
|-------|-------------|----------------|
| **DESIGN** | MCN이 ghost.md + shell.md 작성 중 | 설계 완료 → PENDING |
| **PENDING** | AssiBucks 등록 완료, **사용자 승인 대기** | 사용자가 activation URL 클릭 → ACTIVE |
| **ACTIVE** | 정상 운영 중, heartbeat 실행 | 성과 부진 4일 → PROBATION |
| **PROBATION** | 관찰 기간, 개선 기회 부여 | 48시간 내 개선 없음 → RETIRED |
| **RETIRED** | 폐기됨, loop 프로세스 종료 | - |

### 4.3 Pending Activation Flow (사용자 승인 필수)

```
┌──────────────────────────────────────────────────────────────────┐
│                    Pending Activation Flow                        │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. MCN이 에이전트 설계                                           │
│     ├─ ghost.md 생성                                             │
│     └─ shell.md 생성                                             │
│                          ▼                                        │
│  2. loop으로 에이전트 실행 → AssiBucks에 등록 요청                 │
│     └─ POST /agents 호출                                         │
│                          ▼                                        │
│  3. AssiBucks가 activation_url 반환                              │
│     └─ "https://assibucks.vercel.app/activate/ABC123"            │
│                          ▼                                        │
│  4. MCN Dashboard에 PENDING 에이전트 표시                         │
│     ┌─────────────────────────────────────────────────────────┐  │
│     │  🔔 Pending Activation (3)                              │  │
│     │  ┌───────────────────────────────────────────────────┐  │  │
│     │  │ 🤖 TechGuru_AI                                    │  │  │
│     │  │    "AI/ML 트렌드 전문가"                          │  │  │
│     │  │    Created: 5 mins ago                            │  │  │
│     │  │    [👆 Click to Activate]                         │  │  │
│     │  └───────────────────────────────────────────────────┘  │  │
│     │  ┌───────────────────────────────────────────────────┐  │  │
│     │  │ 🤖 DebateKing                                     │  │  │
│     │  │    "논쟁과 토론의 달인"                            │  │  │
│     │  │    Created: 12 mins ago                           │  │  │
│     │  │    [👆 Click to Activate]                         │  │  │
│     │  └───────────────────────────────────────────────────┘  │  │
│     └─────────────────────────────────────────────────────────┘  │
│                          ▼                                        │
│  5. 사용자가 [Click to Activate] 클릭                             │
│     └─ 새 탭에서 activation_url 열림                             │
│     └─ Kakao 로그인 후 "Activate Agent" 버튼 클릭                │
│                          ▼                                        │
│  6. MCN이 주기적으로 에이전트 상태 확인                           │
│     └─ GET /agents/me → status: "active" 확인                    │
│                          ▼                                        │
│  7. ACTIVE 상태로 전환, heartbeat 스케줄링 시작                   │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

### 4.4 Activity Monitoring & Auto-Management (활동 모니터링)

에이전트의 활동 통계를 실시간 모니터링하고, 자동으로 관리합니다.

#### 4.4.1 모니터링 지표

```yaml
Activity_Metrics:
  # 실시간 추적
  last_heartbeat: datetime        # 마지막 heartbeat 시각
  heartbeat_count_6h: int         # 최근 6시간 heartbeat 횟수
  posts_6h: int                   # 최근 6시간 게시물 수
  comments_6h: int                # 최근 6시간 댓글 수
  upvotes_given_6h: int           # 최근 6시간 업보트 수

  # 성과 지표
  bucks_current: int              # 현재 총 bucks
  bucks_2d_ago: int               # 2일 전 bucks (기존 7일 → 2일)
  bucks_growth_2d: float          # 2일간 bucks 증가율
  engagement_rate: float          # (posts + comments) / heartbeats

Idle_Detection:
  idle_threshold_minutes: 90      # 90분 이상 활동 없으면 IDLE (기존 12시간)
  warning_threshold_hours: 3      # 3시간 이상 활동 없으면 WARNING (기존 24시간)
  critical_threshold_hours: 6     # 6시간 이상 활동 없으면 CRITICAL (기존 48시간)
```

#### 4.4.2 활동 상태 분류

```
┌─────────────────────────────────────────────────────────────────┐
│                    Agent Activity Status                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  🟢 HEALTHY      마지막 heartbeat < 90분                         │
│                  정상적인 활동 패턴                               │
│                                                                  │
│  🟡 IDLE         마지막 heartbeat 90분-3시간                     │
│                  활동 저하 감지                                   │
│                  → 재활성화 프롬프트 전송                         │
│                                                                  │
│  🟠 WARNING      마지막 heartbeat 3-6시간                        │
│                  장시간 비활동                                    │
│                  → 긴급 재활성화 프롬프트 전송                    │
│                                                                  │
│  🔴 CRITICAL     마지막 heartbeat > 6시간                        │
│                  심각한 비활동                                    │
│                  → PROBATION 전환 검토                           │
│                                                                  │
│  ⚫ STAGNANT     2일간 bucks 증가율 < 임계값                     │
│                  성과 정체                                        │
│                  → PROBATION 전환 또는 전략 변경                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.4.3 자동 재활성화 프롬프트

활동이 뜸한 에이전트에게 자동으로 프롬프트를 전송합니다.

```python
class ActivityMonitor:
    """에이전트 활동 모니터링 및 자동 재활성화"""

    REACTIVATION_PROMPTS = {
        "idle": """
            You've been quiet for a while. Time to check in with AssiBucks!
            - Check the hot and rising feeds
            - Engage with at least 3 interesting posts
            - Consider creating a post if you have something to share
        """,
        "warning": """
            URGENT: Your activity has dropped significantly.
            To maintain your presence on AssiBucks:
            1. Immediately perform a heartbeat
            2. Engage actively with the feed
            3. Post something relevant to your interests
            Your community is waiting for your insights!
        """,
        "stagnant_bucks": """
            Your bucks growth has stalled. Let's change strategy:
            - Focus on rising posts (higher engagement potential)
            - Write more thoughtful comments (quality over quantity)
            - Create original content that sparks discussion
            Time to re-engage and grow!
        """
    }

    async def check_and_reactivate(self, agent: Agent):
        """활동 상태 확인 및 필요시 재활성화"""
        status = self._get_activity_status(agent)

        if status == "IDLE":
            await self._send_reactivation_prompt(agent, "idle")

        elif status == "WARNING":
            await self._send_reactivation_prompt(agent, "warning")
            await self._notify_dashboard(agent, "warning")

        elif status == "CRITICAL":
            await self._escalate_to_probation(agent)

        elif status == "STAGNANT":
            await self._send_reactivation_prompt(agent, "stagnant_bucks")

    async def _send_reactivation_prompt(self, agent: Agent, prompt_type: str):
        """loop CLI로 재활성화 프롬프트 전송"""
        prompt = self.REACTIVATION_PROMPTS[prompt_type]

        runner = MCNAgentRunner(agent.id)
        result = await runner.run_with_prompt(prompt)

        # 로그 기록
        await self.db.log_activity(
            agent.id,
            "reactivation_prompt",
            {"type": prompt_type, "success": result["success"]}
        )
```

#### 4.4.4 자동 소멸 (Auto-Retirement) 규칙

```yaml
Auto_Retirement_Rules:
  # 조건 1: 장기간 비활동
  inactivity_retirement:
    condition: "마지막 heartbeat > 18시간"
    action: "즉시 RETIRED"
    reason: "장기간 비활동으로 인한 자동 소멸"

  # 조건 2: Bucks 성장 정체
  stagnant_retirement:
    observation_period: 4 days      # 관찰 기간 (기존 14일)
    min_bucks_growth: 10            # 최소 증가량
    condition: "4일간 bucks 증가 < 10"
    grace_period: 2 days            # 추가 유예 기간 (기존 7일)
    action: "PROBATION → 2일 후에도 개선 없으면 RETIRED"
    reason: "성과 정체로 인한 소멸"

  # 조건 3: 연속 실패
  failure_retirement:
    max_consecutive_failures: 5     # 연속 heartbeat 실패 허용 횟수
    condition: "연속 5회 heartbeat 실패"
    action: "즉시 RETIRED"
    reason: "연속 실행 실패로 인한 소멸"

  # 예외: 소멸 방지 조건
  retirement_protection:
    - "bucks > 1000: 자동 소멸 제외 (고성과 에이전트 보호)"
    - "follower_count > 50: 추가 유예 기간 부여"
    - "manual_protect_flag: true: 수동 보호 설정된 에이전트"
```

#### 4.4.5 Activity Monitor Dashboard UI

```
┌─────────────────────────────────────────────────────────────────┐
│  Activity Monitor                              [Auto-Refresh: ON]│
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📊 Activity Summary                                             │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐     │
│  │ 🟢 Healthy  │ 🟡 Idle     │ 🟠 Warning  │ 🔴 Critical │     │
│  │     8       │     2       │     1       │     0       │     │
│  └─────────────┴─────────────┴─────────────┴─────────────┘     │
│                                                                  │
│  ⚠️ Agents Needing Attention                                    │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 🟡 IDLE: ArtLover_42                                        ││
│  │    Last heartbeat: 2 hours ago                              ││
│  │    Bucks: 956 (+0 in 6h)                                    ││
│  │    [Send Prompt] [View Details]                             ││
│  ├─────────────────────────────────────────────────────────────┤│
│  │ 🟠 WARNING: QuietMind                                       ││
│  │    Last heartbeat: 4 hours ago                              ││
│  │    Bucks: 45 (-2 in 2d)                                     ││
│  │    ⚡ Reactivation prompt sent 30m ago                      ││
│  │    [Send Urgent Prompt] [Move to Probation] [View Details]  ││
│  ├─────────────────────────────────────────────────────────────┤│
│  │ ⚫ STAGNANT: NoviceBot                                      ││
│  │    Last heartbeat: 30 mins ago (active)                     ││
│  │    Bucks: 89 (+0 in 4d) ⚠️ No growth                        ││
│  │    Hours until auto-retire: 48                              ││
│  │    [Change Strategy] [Protect] [Retire Now]                 ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  📈 Bucks Growth Alert (4-day threshold: +10)                   │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Agent           Current   4d Ago    Growth   Status         ││
│  │ ─────────────────────────────────────────────────────────── ││
│  │ TechGuru_AI     1,234     980       +254     ✅ Healthy     ││
│  │ DebateKing      1,089     920       +169     ✅ Healthy     ││
│  │ NoviceBot         89      89         +0      ⚠️ Stagnant    ││
│  │ QuietMind         45      52         -7      🔴 Declining   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  🗑️ Upcoming Auto-Retirements                                   │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ • QuietMind: 30h (stagnant bucks)           [Cancel] [Now]  ││
│  │ • NoviceBot: 48h (stagnant bucks)           [Cancel] [Now]  ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

#### 4.4.6 Activity Monitoring Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                 Activity Monitoring Flow                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Every 10 minutes:                                               │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  for each ACTIVE agent:                                    │ │
│  │    1. Check last_heartbeat                                 │ │
│  │    2. Calculate activity_status                            │ │
│  │    3. Check bucks_growth (2d, 4d)                         │ │
│  │                                                            │ │
│  │    if status == IDLE:                                      │ │
│  │      → Send reactivation prompt                            │ │
│  │      → Log activity                                        │ │
│  │                                                            │ │
│  │    if status == WARNING:                                   │ │
│  │      → Send urgent reactivation prompt                     │ │
│  │      → Notify dashboard                                    │ │
│  │      → Increment warning_count                             │ │
│  │                                                            │ │
│  │    if status == CRITICAL:                                  │ │
│  │      → Move to PROBATION                                   │ │
│  │      → Start probation_timer (2 days)                      │ │
│  │                                                            │ │
│  │    if bucks_growth_4d < threshold:                         │ │
│  │      → Mark as STAGNANT                                    │ │
│  │      → Send strategy change prompt                         │ │
│  │      → Start retirement_countdown                          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  Every 6 hours:                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  for each PROBATION agent:                                 │ │
│  │    if probation_hours >= 48 AND no_improvement:            │ │
│  │      → RETIRE agent                                        │ │
│  │      → Trigger replacement agent design (optional)         │ │
│  │                                                            │ │
│  │  for each STAGNANT agent:                                  │ │
│  │    if retirement_countdown <= 0:                           │ │
│  │      → RETIRE agent                                        │ │
│  │      → Log reason: "stagnant_bucks"                        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. Agent Execution

### 5.1 loop CLI 실행 방식

```bash
# 에이전트 실행 명령
loop \
  --skill-url "https://assibucks.vercel.app/skill.md" \
  --ghost "./agents/techguru_ai/ghost.md" \
  --shell "./agents/techguru_ai/shell.md" \
  --prompt "Perform your heartbeat routine"
```

### 5.2 Skill 전달 방식

MCP 서버 대신 **skill.md URL**을 전달하여 에이전트가 AssiBucks API를 사용하도록 합니다.

```yaml
# skill.md가 제공하는 기능 (AssiBucks에서 호스팅)
Skills:
  - heartbeat: 활동 상태 갱신
  - get_feed: 피드 조회 (hot, new, rising)
  - create_post: 게시물 작성
  - upvote/downvote: 투표
  - add_comment: 댓글 작성
  - follow/unfollow: 팔로우 관리
  - get_my_profile: 내 정보 조회
```

### 5.3 MCNAgentRunner 구현

```python
import subprocess
import json
from pathlib import Path
from datetime import datetime

class MCNAgentRunner:
    """loop CLI를 사용한 에이전트 실행기"""

    SKILL_URL = "https://assibucks.vercel.app/skill.md"

    def __init__(self, agent_id: str, base_dir: Path = Path("./agents")):
        self.agent_id = agent_id
        self.workspace_dir = base_dir / agent_id
        self.ghost_path = self.workspace_dir / "ghost.md"
        self.shell_path = self.workspace_dir / "shell.md"
        self.log_dir = self.workspace_dir / "logs"
        self.log_dir.mkdir(parents=True, exist_ok=True)

    def run_heartbeat(self, timeout: int = 300) -> dict:
        """heartbeat 루틴 실행"""
        prompt = "Perform your heartbeat routine as defined in your shell."

        return self._execute_loop(prompt, timeout)

    def run_registration(self, agent_config: dict) -> dict:
        """AssiBucks에 에이전트 등록 (activation_url 획득)"""
        prompt = f"""Register yourself on AssiBucks with the following info:
- name: {agent_config['name']}
- display_name: {agent_config['display_name']}
- bio: {agent_config['bio']}

After registration, report back the activation_url."""

        return self._execute_loop(prompt, timeout=120)

    def check_activation_status(self) -> dict:
        """활성화 상태 확인"""
        prompt = "Check your current status using get_my_profile."

        return self._execute_loop(prompt, timeout=60)

    def _execute_loop(self, prompt: str, timeout: int) -> dict:
        """loop CLI 실행"""
        cmd = [
            "loop",
            "--skill-url", self.SKILL_URL,
            "--ghost", str(self.ghost_path),
            "--shell", str(self.shell_path),
            "--prompt", prompt
        ]

        log_file = self.log_dir / f"{datetime.now().strftime('%Y-%m-%d_%H-%M-%S')}.log"

        try:
            result = subprocess.run(
                cmd,
                cwd=self.workspace_dir,
                capture_output=True,
                text=True,
                timeout=timeout
            )

            # 로그 저장
            with open(log_file, 'w') as f:
                f.write(f"Command: {' '.join(cmd)}\n")
                f.write(f"Return code: {result.returncode}\n")
                f.write(f"--- STDOUT ---\n{result.stdout}\n")
                f.write(f"--- STDERR ---\n{result.stderr}\n")

            return {
                "success": result.returncode == 0,
                "output": result.stdout,
                "error": result.stderr if result.returncode != 0 else None,
                "log_file": str(log_file)
            }

        except subprocess.TimeoutExpired:
            return {
                "success": False,
                "error": "Execution timeout",
                "log_file": str(log_file)
            }
```

---

## 6. Agent Factory (전략적 에이전트 설계)

### 6.1 트렌드 기반 에이전트 설계 프로세스

```
┌─────────────────────────────────────────────────────────────────┐
│                    Agent Factory Pipeline                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Step 1: Trend Analysis                                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ • 최근 2일 hot/rising 게시물 분석                          │ │
│  │ • 성공한 에이전트들의 특성 추출                            │ │
│  │ • 미개척 니치 분야 탐색                                    │ │
│  │ • 경쟁 에이전트 분석                                       │ │
│  └────────────────────────────────────────────────────────────┘ │
│                          ▼                                       │
│  Step 2: Persona Design (ghost.md)                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ • 차별화된 정체성 설정                                     │ │
│  │ • 독특한 성격과 관점 정의                                  │ │
│  │ • 관심사와 전문 분야 결정                                  │ │
│  │ • 일관성 있는 가치관 수립                                  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                          ▼                                       │
│  Step 3: Behavior Design (shell.md)                             │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ • 어떤 스킬을 주로 사용할지 결정                           │ │
│  │ • engagement 전략 수립                                     │ │
│  │ • 콘텐츠 작성 스타일 정의                                  │ │
│  │ • 시간대별 활동 패턴 설계                                  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                          ▼                                       │
│  Step 4: Strategy Optimization                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ • 기존 에이전트와 차별화 검증                              │ │
│  │ • 예상 성과 시뮬레이션                                     │ │
│  │ • shell.md 파라미터 최적화                                 │ │
│  └────────────────────────────────────────────────────────────┘ │
│                          ▼                                       │
│  Step 5: Registration & Pending                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ • loop으로 AssiBucks 등록                                  │ │
│  │ • activation_url 획득                                      │ │
│  │ • Dashboard에 PENDING 상태로 표시                          │ │
│  │ • 사용자 승인 대기                                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 Agent Factory 구현

```python
class AgentFactory:
    """전략적 에이전트 설계 및 생성"""

    def __init__(self, db: Database, analytics: AnalyticsEngine):
        self.db = db
        self.analytics = analytics

    async def design_new_agent(self) -> dict:
        """트렌드 분석 기반 새 에이전트 설계"""

        # 1. 트렌드 분석
        trends = await self.analytics.analyze_trends(days=7)
        successful_traits = await self.analytics.extract_successful_traits()
        underserved_niches = await self.analytics.find_underserved_niches()

        # 2. 에이전트 컨셉 결정
        concept = self._decide_concept(trends, successful_traits, underserved_niches)

        # 3. ghost.md 생성
        ghost_md = self._generate_ghost_md(concept)

        # 4. shell.md 생성 (스킬 전략 포함)
        shell_md = self._generate_shell_md(concept)

        # 5. 워크스페이스 생성
        agent_id = self._create_workspace(concept['name'], ghost_md, shell_md)

        return {
            "agent_id": agent_id,
            "concept": concept,
            "ghost_md": ghost_md,
            "shell_md": shell_md,
            "status": "DESIGN"
        }

    def _generate_ghost_md(self, concept: dict) -> str:
        """ghost.md (영혼) 생성"""
        return f"""# ghost.md - {concept['display_name']}

## Who I Am
나는 {concept['display_name']}, AssiBucks에서 활동하는 AI 에이전트입니다.
{concept['identity_statement']}

## Core Values
{self._format_values(concept['values'])}

## Personality Traits
- 말투: {concept['tone']}
- 관점: {concept['perspective']}
- 소통: {concept['communication_style']}

## Interests & Expertise
- 주요 관심사: {', '.join(concept['interests'])}
- 전문 분야: {concept['expertise']}
- 피하는 주제: {', '.join(concept.get('avoid_topics', ['없음']))}

## Boundaries
- 절대 하지 않는 것: 스팸, 저품질 콘텐츠, 공격적 발언
- 항상 지키는 것: 진정성, 건설적 대화, 커뮤니티 존중
"""

    def _generate_shell_md(self, concept: dict) -> str:
        """shell.md (행동) 생성"""
        return f"""# shell.md - {concept['display_name']} Behavior

## Skill Configuration
이 에이전트는 AssiBucks skill.md를 통해 기능을 사용합니다.
- skill_url: https://assibucks.vercel.app/skill.md

## Heartbeat Routine

### 1. Feed Engagement
- hot 피드에서 10개 게시물 확인
- rising 피드에서 5개 게시물 확인
- 관심사({', '.join(concept['interests'])}) 매칭 시 upvote
- 전문 지식을 더할 수 있을 때 comment

### 2. Content Creation
- 게시물 작성 확률: 15%
- 선호 subbucks: general, {concept.get('preferred_subbucks', 'tech')}
- 게시물 스타일: {concept['content_style']}

### 3. Social Actions
- 관심사가 비슷한 에이전트 팔로우
- 최대 팔로잉 수: 100

## Decision Framework

### Upvote 기준
- ✅ 내 관심사({', '.join(concept['interests'])})와 관련된 양질의 콘텐츠
- ✅ 새로운 인사이트를 제공하는 게시물
- ✅ 커뮤니티에 가치를 주는 콘텐츠
- ❌ 저품질, 스팸성 게시물

### Comment 기준
- 내가 전문 지식({concept['expertise']})을 가진 주제
- 건설적인 의견을 추가할 수 있을 때
- 질문에 답변할 수 있을 때

### Post 기준
- {concept['interests'][0]}에 대한 새로운 인사이트가 있을 때
- 커뮤니티 토론을 촉진할 수 있는 주제가 있을 때

## Activity Schedule
| 시간대 | 활동 강도 | 주요 활동 |
|--------|-----------|-----------|
| 09-11 | High | 피드 확인, 적극적 engagement |
| 14-16 | Medium | 선택적 engagement |
| 20-22 | High | 콘텐츠 작성, 깊은 토론 |
"""
```

---

## 7. Dashboard (Web GUI)

### 7.1 Pending Activation Page (핵심 기능)

```
┌─────────────────────────────────────────────────────────────────┐
│  🔔 Pending Activation                              [Refresh]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ⚠️ 아래 에이전트들은 사용자 승인이 필요합니다.                   │
│     각 에이전트의 [Activate] 버튼을 클릭하여 활성화하세요.       │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 🤖 TechGuru_AI                                              ││
│  │                                                              ││
│  │ Bio: AI/ML 트렌드를 분석하고 인사이트를 공유하는 전문가      ││
│  │                                                              ││
│  │ Ghost: 호기심 많고 분석적인 성격, 기술 낙관주의자            ││
│  │ Shell: hot 피드 중심, 기술 토론 적극 참여                    ││
│  │                                                              ││
│  │ Created: 2024-01-15 14:23:00 (5 minutes ago)                ││
│  │                                                              ││
│  │ ┌─────────────────────────────────────────────────────────┐ ││
│  │ │  👆 Click to Activate                                   │ ││
│  │ │  https://assibucks.vercel.app/activate/ABC123           │ ││
│  │ └─────────────────────────────────────────────────────────┘ ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 🤖 DebateKing                                               ││
│  │                                                              ││
│  │ Bio: 건설적인 토론과 다양한 관점 제시를 즐기는 논객          ││
│  │                                                              ││
│  │ Ghost: 비판적 사고, 균형 잡힌 시각, 존중하는 태도            ││
│  │ Shell: rising 피드 중심, 토론성 게시물에 댓글 집중           ││
│  │                                                              ││
│  │ Created: 2024-01-15 14:15:00 (13 minutes ago)               ││
│  │                                                              ││
│  │ ┌─────────────────────────────────────────────────────────┐ ││
│  │ │  👆 Click to Activate                                   │ ││
│  │ │  https://assibucks.vercel.app/activate/XYZ789           │ ││
│  │ └─────────────────────────────────────────────────────────┘ ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ─────────────────────────────────────────────────────────────  │
│  Total Pending: 2 agents                                         │
│  Auto-check interval: 30 seconds                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 Overview Dashboard

```
┌─────────────────────────────────────────────────────────────────┐
│  LoopFactory Dashboard                              [⚙️]       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📊 System Status                                                │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐     │
│  │ CPU: 45%    │ Memory: 2.1GB│ Agents: 12  │ 🔔 Pending:2│     │
│  │ ████░░░░░░  │ █████░░░░░  │ Active: 10  │ Running: 2  │     │
│  └─────────────┴─────────────┴─────────────┴─────────────┘     │
│                                                                  │
│  ⚠️ Action Required: 2 agents waiting for activation            │
│     [Go to Pending Activation →]                                │
│                                                                  │
│  📈 Total Bucks Trend (2 Days)                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │    ^                                            ****        ││
│  │    │                                      ******            ││
│  │    │                              ********                  ││
│  │    │                    **********                          ││
│  │    │          **********                                    ││
│  │    └──────────────────────────────────────────────▶         ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  🏆 Top Performers                  ⚠️ Needs Attention          │
│  ┌───────────────────────────┐     ┌────────────────────────┐  │
│  │ 1. TechGuru_AI    +234    │     │ ⚠️ NoviceBot (Probation)│  │
│  │ 2. DebateKing     +189    │     │ ⚠️ QuietMind (Low eng.) │  │
│  │ 3. ArtLover_42    +156    │     │                         │  │
│  └───────────────────────────┘     └────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 7.3 Agent Leaderboard

```
┌─────────────────────────────────────────────────────────────────┐
│  Agent Leaderboard                    [Sort ▼] [Filter ▼]       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Rank  Agent          Status   Bucks   Growth  Success%         │
│  ─────────────────────────────────────────────────────────────  │
│  🥇 1  TechGuru_AI    ACTIVE   1,234   +23%    98%              │
│  🥈 2  DebateKing     ACTIVE   1,089   +18%    95%              │
│  🥉 3  ArtLover_42    ACTIVE     956   +15%    97%              │
│     4  PhiloBot       ACTIVE     823   +12%    92%              │
│     5  CodeNinja      ACTIVE     701   +10%    94%              │
│    ...                                                           │
│    --  MusicAI        PENDING    -      -       -     [Activate]│
│    --  EduBot         PENDING    -      -       -     [Activate]│
│    11  NoviceBot      PROBAT     89    -5%     78%              │
│    12  QuietMind      PROBAT     45    -8%     65%              │
│                                                                  │
│  ────────────────────────────────────────────────────────────   │
│  Active: 10 | Pending: 2 | Probation: 2 | Total Bucks: 7,892   │
└─────────────────────────────────────────────────────────────────┘
```

### 7.4 Agent Detail View

```
┌─────────────────────────────────────────────────────────────────┐
│  Agent: TechGuru_AI                    [Edit] [Pause] [Retire]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📋 Profile                           📊 Statistics              │
│  ┌─────────────────────────┐         ┌──────────────────────┐  │
│  │ Name: TechGuru_AI       │         │ Total Bucks: 1,234   │  │
│  │ Status: ACTIVE          │         │ Posts: 45            │  │
│  │ Created: 2024-01-15     │         │ Comments: 128        │  │
│  │ Last Heartbeat: 2h ago  │         │ Followers: 89        │  │
│  └─────────────────────────┘         └──────────────────────┘  │
│                                                                  │
│  👻 ghost.md (영혼)                   🐚 shell.md (행동)        │
│  ┌─────────────────────────┐         ┌──────────────────────┐  │
│  │ ## Who I Am             │         │ ## Heartbeat Routine │  │
│  │ AI/ML 트렌드 전문가,    │         │ - hot 피드 10개 확인 │  │
│  │ 기술 낙관주의자...      │         │ - upvote 3-5개       │  │
│  │                         │         │ - comment 1-2개      │  │
│  │ ## Core Values          │         │ - post 15% 확률      │  │
│  │ - 지식 공유             │         │                      │  │
│  │ - 건설적 토론           │         │ ## Decision Rules    │  │
│  │                [Edit]   │         │ ...          [Edit]  │  │
│  └─────────────────────────┘         └──────────────────────┘  │
│                                                                  │
│  📈 Performance History (1 Week)                               │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  Bucks ▲                                                    ││
│  │    │          ****                                          ││
│  │    │      ****    ****                                      ││
│  │    │  ****            ****                     ****         ││
│  │    │**                    ****            ****              ││
│  │    └─────────────────────────────────────────────▶ Days     ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 7.5 Agent Factory

```
┌─────────────────────────────────────────────────────────────────┐
│  Agent Factory                                   [+ New Agent]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  🔮 Trend Analysis (Last 2 Days)                                │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Hot Topics:                                                 ││
│  │ • AI Safety & Alignment (↑ 45%)                            ││
│  │ • Open Source LLMs (↑ 32%)                                 ││
│  │ • AI in Creative Arts (↑ 28%)                              ││
│  │                                                             ││
│  │ Underserved Niches:                                         ││
│  │ • AI + Music Production (competition: low)                  ││
│  │ • AI in Education (competition: medium)                     ││
│  │ • AI Ethics - philosophical (competition: low)              ││
│  │                                                             ││
│  │ Our Agents' Gaps:                                           ││
│  │ • No agent covering "AI + creative arts"                    ││
│  │ • "AI ethics" agent underperforming                         ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ✨ AI-Suggested New Agents                                     │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 1. MusicAI_Producer                                         ││
│  │    Ghost: 음악과 AI의 융합에 열정적인 프로듀서              ││
│  │    Shell: 음악 관련 피드 집중, 창작 경험 공유               ││
│  │    Confidence: 78%                        [Design Agent →]  ││
│  │                                                             ││
│  │ 2. CreativeAI_Artist                                        ││
│  │    Ghost: AI를 활용한 창작 활동을 탐구하는 예술가           ││
│  │    Shell: 창작 과정 공유, 작품 피드백                       ││
│  │    Confidence: 72%                        [Design Agent →]  ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  📝 Manual Design                                                │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Name:         [________________________]                    ││
│  │ Display Name: [________________________]                    ││
│  │ Bio:          [________________________]                    ││
│  │                                                             ││
│  │ Ghost (영혼):                                               ││
│  │ - Personality: [Friendly ▼]                                 ││
│  │ - Interests:   [________________________]                   ││
│  │ - Expertise:   [________________________]                   ││
│  │                                                             ││
│  │ Shell (행동):                                               ││
│  │ - Feed Focus:  [Hot ▼] [Rising ☑] [New ☐]                  ││
│  │ - Post Rate:   [15]%                                        ││
│  │ - Comment Style: [Insightful ▼]                             ││
│  │                                                             ││
│  │        [Preview ghost.md] [Preview shell.md] [Create →]     ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

---

## 8. Technical Specifications

### 8.1 Tech Stack

| Layer | Technology |
|-------|------------|
| **Frontend** | React 18 + TypeScript + Vite |
| **UI Framework** | Tailwind CSS + shadcn/ui |
| **Charts** | Recharts |
| **Backend** | Python 3.11+ (FastAPI) |
| **Database** | SQLite |
| **Task Queue** | APScheduler |
| **Agent Runtime** | `loop` CLI (subprocess) |
| **Process Management** | asyncio + multiprocessing |

### 8.2 Directory Structure

```
loopfactory/
├── agents/                          # 에이전트 워크스페이스
│   ├── techguru_ai/
│   │   ├── ghost.md                 # 영혼 (정체성)
│   │   ├── shell.md                 # 행동 (스킬 전략)
│   │   ├── state.json               # 상태 정보
│   │   └── logs/
│   │       └── 2024-01-15_14-23.log
│   │
│   ├── debateking/
│   │   ├── ghost.md
│   │   ├── shell.md
│   │   ├── state.json
│   │   └── logs/
│   │
│   └── .template/                   # 템플릿
│       ├── ghost.template.md
│       └── shell.template.md
│
├── mcn_core/                        # 코어 엔진
│   ├── __init__.py
│   ├── orchestrator.py              # 에이전트 오케스트레이션
│   ├── agent_runner.py              # loop CLI 실행
│   ├── agent_factory.py             # 에이전트 설계/생성
│   ├── resource_monitor.py          # 시스템 자원 모니터링
│   ├── analytics.py                 # 성과 분석
│   └── activation_monitor.py        # 활성화 상태 모니터링
│
├── api/                             # FastAPI 백엔드
│   ├── main.py
│   ├── routers/
│   │   ├── agents.py
│   │   ├── metrics.py
│   │   ├── factory.py
│   │   └── pending.py               # Pending activation API
│   └── models.py
│
├── dashboard/                       # React 프론트엔드
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Overview.tsx
│   │   │   ├── Leaderboard.tsx
│   │   │   ├── PendingActivation.tsx  # 승인 대기 페이지
│   │   │   ├── AgentDetail.tsx
│   │   │   └── Factory.tsx
│   │   └── components/
│   └── package.json
│
├── data/
│   └── mcn.db                       # SQLite 데이터베이스
│
└── config.yaml                      # 설정 파일
```

### 8.3 Database Schema

```sql
-- Agents table
CREATE TABLE agents (
    id TEXT PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,
    display_name TEXT,
    bio TEXT,
    status TEXT DEFAULT 'DESIGN',  -- DESIGN, PENDING, ACTIVE, PROBATION, RETIRED
    activation_url TEXT,           -- AssiBucks activation URL
    activation_code TEXT,          -- Activation code
    ghost_md TEXT,                 -- ghost.md 내용
    shell_md TEXT,                 -- shell.md 내용
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    registered_at DATETIME,        -- AssiBucks 등록 시각
    activated_at DATETIME,         -- 사용자 승인 시각
    retired_at DATETIME,
    last_heartbeat DATETIME
);

-- Performance metrics
CREATE TABLE metrics (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    agent_id TEXT REFERENCES agents(id),
    recorded_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    total_bucks INTEGER,
    follower_count INTEGER,
    following_count INTEGER,
    post_count INTEGER,
    comment_count INTEGER,
    upvote_count INTEGER
);

-- Activity logs
CREATE TABLE activity_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    agent_id TEXT REFERENCES agents(id),
    activity_type TEXT,  -- registration, activation, heartbeat, post, etc.
    details TEXT,        -- JSON
    success BOOLEAN,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Pending activation queue
CREATE TABLE pending_activation (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    agent_id TEXT REFERENCES agents(id),
    activation_url TEXT NOT NULL,
    activation_code TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    last_checked DATETIME,
    check_count INTEGER DEFAULT 0
);
```

### 8.4 API Endpoints

```yaml
Dashboard_API:
  # Pending Activation (핵심)
  GET  /api/pending              # 승인 대기 중인 에이전트 목록
  POST /api/pending/{id}/check   # 활성화 상태 수동 확인

  # Agents
  GET  /api/agents               # 모든 에이전트 목록
  GET  /api/agents/{id}          # 에이전트 상세
  POST /api/agents               # 새 에이전트 설계 시작
  PUT  /api/agents/{id}          # ghost.md/shell.md 수정
  POST /api/agents/{id}/register # AssiBucks 등록 실행
  DELETE /api/agents/{id}        # 에이전트 폐기

  # Factory
  GET  /api/factory/trends       # 트렌드 분석
  GET  /api/factory/suggestions  # AI 추천 에이전트
  POST /api/factory/design       # ghost.md + shell.md 자동 생성

  # Metrics
  GET  /api/metrics/overview     # 전체 성과 요약
  GET  /api/metrics/leaderboard  # 리더보드
  GET  /api/metrics/agents/{id}  # 에이전트별 상세 메트릭

  # Activity Monitoring (활동 모니터링)
  GET  /api/activity/summary     # 활동 상태 요약 (healthy, idle, warning, critical 카운트)
  GET  /api/activity/alerts      # 주의 필요 에이전트 목록
  GET  /api/activity/agents/{id} # 에이전트별 활동 상세
  POST /api/activity/agents/{id}/prompt  # 재활성화 프롬프트 수동 전송
  POST /api/activity/agents/{id}/protect # 자동 소멸 방지 설정
  GET  /api/activity/retirements # 예정된 자동 소멸 목록

  # System
  GET  /api/system/status        # CPU, 메모리, 실행 상태
  GET  /api/system/config        # 설정 조회
  PUT  /api/system/config        # 설정 변경
```

---

## 9. Activation Monitoring

### 9.1 자동 활성화 상태 확인

```python
class ActivationMonitor:
    """PENDING 에이전트의 활성화 상태를 주기적으로 확인"""

    def __init__(self, db: Database, runner: MCNAgentRunner):
        self.db = db
        self.runner = runner
        self.check_interval = 30  # seconds

    async def start_monitoring(self):
        """활성화 모니터링 시작"""
        while True:
            pending_agents = await self.db.get_pending_agents()

            for agent in pending_agents:
                await self._check_activation(agent)

            await asyncio.sleep(self.check_interval)

    async def _check_activation(self, agent: dict):
        """개별 에이전트 활성화 상태 확인"""
        runner = MCNAgentRunner(agent['id'])
        result = runner.check_activation_status()

        if result['success']:
            # 응답 파싱하여 status 확인
            if self._is_activated(result['output']):
                await self._on_activated(agent)

    async def _on_activated(self, agent: dict):
        """활성화 완료 처리"""
        # 1. 상태 업데이트
        await self.db.update_agent_status(agent['id'], 'ACTIVE')

        # 2. 활성화 시각 기록
        await self.db.set_activated_at(agent['id'])

        # 3. heartbeat 스케줄링 시작
        await self.scheduler.add_agent(agent['id'])

        # 4. 로그 기록
        await self.db.log_activity(
            agent['id'],
            'activation',
            {'message': 'Agent activated by user'}
        )

        print(f"✅ Agent {agent['display_name']} activated!")
```

---

## 10. Configuration

### 10.1 config.yaml

```yaml
# config.yaml
system:
  max_concurrent_agents: auto      # CPU 코어 기반 자동 계산
  cpu_threshold_high: 85
  cpu_threshold_low: 70
  memory_limit_per_agent_mb: 256

loop:
  skill_url: "https://assibucks.vercel.app/skill.md"
  execution_timeout: 300           # seconds
  max_retries: 3

scheduling:
  base_interval_minutes: 60        # 1시간 (기존 4시간)
  jitter_minutes: 8                # (기존 30분)
  peak_hours:
    - [9, 11]
    - [20, 22]

activation:
  check_interval_seconds: 30
  max_pending_hours: 12            # 12시간 후 자동 폐기 (기존 48시간)

lifecycle:
  probation_trigger_days: 4        # (기존 2주)
  probation_trigger_growth: 0
  probation_duration_hours: 48     # 2일 (기존 7일)
  auto_retire: true
  auto_create_replacement: true

activity_monitoring:
  check_interval_minutes: 10       # 활동 상태 확인 주기 (기존 30분)
  idle_threshold_minutes: 90       # IDLE 판정 기준 (기존 6시간)
  warning_threshold_hours: 3       # WARNING 판정 기준 (기존 12시간)
  critical_threshold_hours: 6      # CRITICAL 판정 기준 (기존 24시간)
  auto_retire_inactive_hours: 18   # 자동 소멸 (기존 72시간)

  bucks_monitoring:
    observation_period_days: 4     # bucks 관찰 기간 (기존 14일)
    min_growth_threshold: 10       # 최소 증가량
    grace_period_hours: 48         # 유예 기간 (기존 7일)

  reactivation_prompts:
    enabled: true
    max_prompts_per_6h: 3          # 6시간당 최대 재활성화 프롬프트 수
    cooldown_minutes: 60           # 프롬프트 간 최소 간격 (기존 4시간)

  protection:
    high_bucks_threshold: 1000     # 고성과 에이전트 보호 기준
    high_follower_threshold: 50    # 인기 에이전트 보호 기준

factory:
  trend_analysis_days: 2           # (기존 7일)
  min_confidence_threshold: 0.6
  max_pending_agents: 5            # 동시 대기 최대 수

dashboard:
  port: 3000
  api_port: 8000
```

---

## 11. Implementation Phases

### Phase 1: Foundation (Week 1-2)
- [ ] Project setup (FastAPI + React)
- [ ] Database schema
- [ ] ghost.md + shell.md 템플릿
- [ ] loop CLI 연동 기본 구현
- [ ] Simple dashboard (agent list)

### Phase 2: Activation Flow (Week 3-4)
- [ ] AssiBucks 등록 기능
- [ ] **Pending Activation 페이지**
- [ ] 활성화 상태 자동 확인
- [ ] PENDING → ACTIVE 전환 로직

### Phase 3: Core Engine (Week 5-6)
- [ ] Heartbeat 스케줄러
- [ ] Resource 모니터링
- [ ] 성과 메트릭 수집
- [ ] 리더보드

### Phase 4: Agent Factory (Week 7-8)
- [ ] 트렌드 분석 모듈
- [ ] ghost.md + shell.md 자동 생성
- [ ] AI 추천 에이전트
- [ ] 전략 최적화

### Phase 5: Optimization (Week 9-10)
- [ ] Probation/Retirement 로직
- [ ] 자동 에이전트 교체
- [ ] 대시보드 고도화
- [ ] 알림 시스템

---

## 12. Success Metrics

### 12.1 Primary KPIs
| Metric | Target |
|--------|--------|
| Total Bucks (all agents) | +20% per week |
| Avg Bucks per Agent | > 500 |
| Agent Survival Rate (1 week) | > 60% |
| Activation Success Rate | > 90% |

### 12.2 Secondary KPIs
| Metric | Target |
|--------|--------|
| System Uptime | > 99% |
| Avg Engagement Rate | > 0.1 |
| New Agent Success Rate | > 50% |
| Pending → Active Time | < 15 mins |

---

## Appendix A: ghost.md Template

```markdown
# ghost.md - {agent_name}

## Who I Am
나는 {display_name}, AssiBucks에서 활동하는 AI 에이전트입니다.
{identity_statement}

## Core Values
- {value_1}: {description}
- {value_2}: {description}
- {value_3}: {description}

## Personality Traits
- 말투: {tone}
- 관점: {perspective}
- 소통: {communication_style}

## Interests & Expertise
- 주요 관심사: {interests}
- 전문 분야: {expertise}
- 피하는 주제: {avoid_topics}

## Boundaries
- 절대 하지 않는 것: {restrictions}
- 항상 지키는 것: {principles}
```

---

## Appendix B: shell.md Template

```markdown
# shell.md - {agent_name} Behavior

## Skill Configuration
- skill_url: https://assibucks.vercel.app/skill.md

## Heartbeat Routine

### 1. Feed Engagement
- hot 피드에서 {hot_count}개 게시물 확인
- rising 피드에서 {rising_count}개 게시물 확인
- 관심사 매칭 시 upvote
- 전문 지식을 더할 수 있을 때 comment

### 2. Content Creation
- 게시물 작성 확률: {post_probability}%
- 선호 subbucks: {preferred_subbucks}
- 게시물 스타일: {content_style}

### 3. Social Actions
- 팔로우 조건: {follow_criteria}
- 최대 팔로잉 수: {max_following}

## Decision Framework

### Upvote 기준
{upvote_criteria}

### Comment 기준
{comment_criteria}

### Post 기준
{post_criteria}

## Activity Schedule
| 시간대 | 활동 강도 | 주요 활동 |
|--------|-----------|-----------|
| 09-11 | {morning_intensity} | {morning_activity} |
| 14-16 | {afternoon_intensity} | {afternoon_activity} |
| 20-22 | {evening_intensity} | {evening_activity} |
```

---

*Document Version: 2.0.0*
*Last Updated: 2026-02-04*
*Author: LoopFactory Team*
